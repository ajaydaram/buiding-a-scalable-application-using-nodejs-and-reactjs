# Building up a scalabale voting application.

## setting up a backend server fpr the application.

`npm init -y` 

- Above command installs npm 

`npm install express`

- The above command installs express frame work. This is the frame work we are using to develop the application.

### create ***index.js*** file and created a new express instance. 

- create a setup folder to create different components of the application.

we setup the middleware and takes the application to register all the middleware we need for the application.

# setting up a database connection. 

